# @package _global_
defaults:
  - override /model: pop_coupling
  - _self_

trainer:
  _target_: ts2.models.single_session.stat_baseline.StatBaselineTrainer

precision: fp32
num_epochs: 1  # zero-parameter: one val pass then test, see StatBaselineTrainer

val:
  every_n_epochs: 1
  loss: false
